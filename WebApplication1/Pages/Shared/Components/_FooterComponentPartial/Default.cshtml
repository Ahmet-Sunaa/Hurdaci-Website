@model WebApplication1.DAL.ViewModel.FooterViewModel
<div class="container-fluid bg-dark text-light footer mt-5 pt-5 wow fadeIn" data-wow-delay="0.1s">
    <div class="container py-5">
        <div class="row g-5">
            
            <div class="col-lg-3 col-md-6">
                <h4 class="text-light mb-4">@Model.Contacts[0].Title</h4>
                <p class="mb-2"><i class="fa fa-map-marker-alt me-3"></i>@Model.Contacts[0].Adress</p>
                <p class="mb-2"><i class="fa fa-phone-alt me-3"></i>@Model.Contacts[0].Phone</p>
                <!--<p class="mb-2"><i class="fa fa-envelope me-3"></i>info@example.com</p>-->
                @*
                <div class="d-flex pt-2">
                    <a class="btn btn-outline-light btn-social" href=""><i class="fab fa-twitter"></i></a>
                    <a class="btn btn-outline-light btn-social" href=""><i class="fab fa-facebook-f"></i></a>
                    <a class="btn btn-outline-light btn-social" href=""><i class="fab fa-youtube"></i></a>
                    <a class="btn btn-outline-light btn-social" href=""><i class="fab fa-linkedin-in"></i></a>
                </div>
                *@
            </div>
            
            <div class="col-lg-3 col-md-6">
                <h4 class="text-light mb-4">Hurdalar</h4>
                @foreach(var item in Model.Scraps){
                    <a class="btn btn-link" href="">@item.Name</a>
                }
                
            </div>
        </div>
    </div>
</div>